## 更新

本版是 **2.0.1**，相对 `2.0.0` 是三件事：**英雄似然判定修好**（原来判据用错、顺序也反了）、
**资料包打包新增图片重编码**（可选，用来把包压小）、以及仓库里多了一份 **图文使用手册**。

### 英雄「最大 / 最小似然」判定修正

原来只有一条判据，而且用错了：

- **判据顺序反了**：`computeLikelihood()` 没有先比回忆卡张数，而是直接拿「轨道推进格数」排序。
  于是「卡少但推得深」的英雄会被误判成最大似然——2 张卡各推到第 3 格（共 6 格）会输给
  1 张卡推到第 10 格（10 格）。
- **格数也算错了**：数格子时把记忆节点所在的格子 `continue` 掉了，而节点固定落在第 3/7/10 格，
  所以推到第 10 格只算 7 格。

现在按正确口径分两级判定：

1. **回忆卡张数**（宿命回忆 `fatedMnemos` 不算）——多者「最大似然」、少者「最小似然」；
2. 张数**完全相同**时，才比**轨道上已推进的格数**（1–10 那排格子，节点格同样计入）；
3. 两级都平局就不打标记。

单张卡推进值上限按 10 格截断，避免存档里的异常值影响比较。

### 数据修正：`c5_07`「他说出了真相」

这张卡的节点位置在 hero 页与主控台两份数据里不一致（`[5,9,14]` vs `[3,7,10]`），
会让主控台的「回忆突破」提醒到第 5 格才出现、而英雄记录表第 3 格就亮灯。现已统一为 `[3,7,10]`。
全表 46 张回忆卡的节点位置现在都是第 3/7/10 格。

### 资料包打包：图片重编码（可选）

`build_fan_pack.py` / `build_official_pack.py` 新增 `--image-quality`：

- 传 40–95 的质量值（常用 `85`）时，打包过程中把图片重新编码以缩小体积；
- 不传就完全不重编，保持历史行为；
- 成员名不变，透明图走调色板 PNG，**收益不够的画保持原样**；
- `--image-quality-keep` / `--image-quality-keep-file` 是豁免清单：命中的图原样进包。
  仓库带了 `asset-studio/pack-shrink-keep.txt`，把决战版图与使徒的完整面板 / Trait 大卡排除在外，
  避免重编码后细节受损；
- 重编码过的成员，清单里的 `sha256` 与大小按**新字节**记录，校验逻辑已同步。

同时修了官中覆盖图（`official-assets/`）的匹配：支持后缀不同（`CARD.png` 配 `CARD.jpg` 目标）
与按资源目录缩短一级的路径。

### 图文使用手册

仓库新增 `docs/guide/`：一份「从下载安装到使用细节」的图文教程，含 21 处真实界面截图
（`ATO-Assistant-图文使用手册.docx` / `.html` / `.md`）。截图本身受 `.gitignore` 的 `*.png`
规则约束不进仓库，docx 里图片已内嵌、可独立阅读。

## 下载

Windows、macOS、Android 请下载下方对应安装包。便携版请解压到新目录，并保留旧版本的 `data/` 和本地素材
目录（图片、故事数据、`assets/bgm/` 音频都不在安装包里，需要自备）。

本版是补丁版，**存档与资料包格式都没有变化**，可以直接覆盖升级：解压到新目录后把旧目录的
`data/` 与本地素材目录搬过去即可。

Docker 镜像同步发布 `2.0.1` 标签，并把 `latest` 移到本版：

```bash
docker compose pull
docker compose up -d
```

Android 沿用现有签名策略：未配置发布签名密钥时提供 Debug 签名 APK。不同构建的签名可能不一致，无法保证
覆盖升级；卸载会删除应用内账号、存档和素材，升级前请确认数据可恢复。

资料包为本地私有产物，不进仓库、不进安装包；仓库的 `.gitignore` 已排除 `*.atopack` 与
`official-assets/`。

## 验证

- 仓库前端回归 **10 个测试文件 / 95 项全部通过**。本版新增
  `tests/hero-likelihood.test.cjs`（12 项），覆盖：主判据是回忆卡张数、次判据是轨道格数、
  「卡少但推得深」不被误判、「节点格计入」、宿命回忆不计入、两级平局不打标记、
  轨道格数上限保护，以及两条数据一致性断言（所有回忆卡 `thresholds` 都是 `[3,7,10]`、
  hero 页与主控台的回忆卡节点表逐卡一致）。
- 全表核对：46 张回忆卡的节点位置现均为第 3/7/10 格，hero 页与主控台两份表 **0 处差异**。
- 内联脚本语法：`hero/index.html`、`index.html` 的 `<script>` 块编译通过（新增
  `tools/check-inline-js.mjs`）。
- 资料包打包器的改动由 `asset-studio/tests/` 下的用例与打包实测覆盖；本版**未重跑**完整
  官方版打包（`--image-quality` 为新选项，建议先用 `--dry-run` 看体积与条目数再正式打包）。
- Windows、macOS、Android 安装包与 Docker 镜像由发布流程构建，结果以本页附件与 Actions 记录为准；
  未进行 Android、macOS 或树莓派实机安装验证，也未用真机导入过本版资料包。
