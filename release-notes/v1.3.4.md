## 更新

修复迷宫机牛 AIBP：特殊 BP 洗入牌堆后，主界面卡背顺序和第二屏卡背按普通 BP3 显示，不再标成 X。

这张卡的暴击 / 击败结算不变：仍会移出游戏，不会按 BP3 关键一击处理。

**正在进行迷宫机牛决战的玩家建议更新。**

本版本同时包含 1.3.3 的 Android 资源导入名单修正。Windows、macOS 和 Docker 用户从 1.3.2 升级即可同时获得这两项。

## 下载

Windows、macOS、Android 请下载下方对应安装包。便携版请解压到新目录，并保留旧版本的 `data/` 和本地素材目录。

Docker 的 `latest` 标签同步更新至 1.3.4。如果此前在 `.env` 中固定了版本，请改为 `ATO_VERSION=1.3.4`，然后运行：

```bash
docker compose pull
docker compose up -d
```

Android 沿用现有签名策略：未配置发布签名密钥时提供 Debug 签名 APK。不同构建的签名可能不一致，无法保证覆盖升级；卸载会删除应用内账号、存档和素材，升级前请确认数据可恢复。

游戏图片、完整故事正文和 `.atopack` 资源包仍由本地资料提供，不包含在公开安装包中。

## 验证

- 迷宫机牛特殊 BP 卡背显示测试通过：`node tools/test-aibp-labyrinthauros-special-bp-back.cjs`。洗入后顺序与组成统计均显示为 3，不出现 X；翻面使用 BP III 牌背图。旧存档里只有 `specialBp`、没有 `backLevel` 的卡同样按 BP3 显示。
- BP III 击败 / 关键一击回归测试通过：`node tools/test-aibp-bp3-defeat.cjs`。特殊 BP 不走该路径，BP3 行为未改。
- 发布公告与附件规则检查通过：`python tools/test_release_assets.py`。
- Windows、macOS、Android 安装包和 Docker 镜像由发布流程构建，结果以本页附件和 Actions 记录为准；未进行 Android、macOS 或树莓派实机安装验证。
